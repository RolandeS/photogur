<h1>Pictures (<%= @pictures.length %>)</h1>
<%= link_to "New Picture", "/pictures/new" %>

<ul>
	<% @pictures.each do |pic| %>
		<li><%= pic.title %>, <%= pic.artist %></li>
		<%= link_to image_tag(pic.url), "/pictures/#{pic.id}" %>
		<!-- <img src = "<%= pic.url %>"> -->
	<% end %>
</ul>